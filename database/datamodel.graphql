type Link {
  id: ID! @unique
  description: String!
  url: String!
  postedBy: User # shouldn't this be User! ?
}

type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  name: String
  links: [Link!]!
}
